( function ( app, undefined ) {

    'use strict';

    /**
     * App constants.
     *
     * @provide app.core.Constants
     *
     * @require app.core
     */
    app.core.Constants = {

        USER_ID: "userId"

    };

} )( app );