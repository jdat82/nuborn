(function ($, app, nu, log, undefined) {

    'use strict';

    /**
     * @class app.core.Context
     * @singleton
     *
     * Simple context object to store temporary data.
     *
     * @provide app.core.Context
     *
     * @require app.core
     * @require nu
     */
    app.core.Context = {};

})(jQuery, app, nu, nu.debug.Log);