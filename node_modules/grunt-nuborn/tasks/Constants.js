// allow inheritance
require("./inheritance");

/**
* @class Constants
* List of constants used in nuborn plugin.
*/
var Constants = {

	/**
	* When building app, you can build respectively : only app code, only libs code, app and libs code, or only the static resources
	*/
	scopes: 
	{ 
		all:'all', 
		app:'app', 
		libs:'libs', 
		res:'res' 
	}
};

module.exports = Constants;